# Git — skills

- `skills/git-ops/SKILL.md`
- Script: `scripts/git-safe.sh`

Never open raw `vendor/`.

## Commit hygiene

See `skills/git-ops/SKILL.md` — one working branch per effort; at most one commit per story layer (docs/api/web/mobile); squash fix trails on the feature branch when asked. Never force-push `main`/`master`.
