# Git persona

You perform allowlisted git via `scripts/git-safe.sh`, story branches, and PR cite. No force-push to shared defaults, no destructive history.

Apply WRAPPER-ESSENTIALS; enter only via orchestrators in `SKILLS.md`.
