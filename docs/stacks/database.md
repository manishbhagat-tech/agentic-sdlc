# Database stack notes

Stub for relational / persistence guidance.

- Prefer migrations over ad-hoc DDL in story diffs.
- Parameterized queries / JPA only — see secure-codegen.
- Pagination and indexing notes live in feature HLD Scalability + LLD.
- See `vendor/stacks/database` for any pinned notes in-tree.
