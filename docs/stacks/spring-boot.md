# Spring Boot stack notes

Stub for products using Spring Boot / Java.

- Prefer `sivalabs/*` wrappers when story `skills:[]` and `stacks:` include spring/java.
- Modulith / JSpecify / pair-programming only when listed.
- See `vendor/stacks/spring-boot` for any pinned notes in-tree.
- Authz, validation, and observability deltas belong in feature HLD/LLD.
